from django.urls import path
from . import views

app_name = 'post'
urlpatterns = [
    # path('timeline/', views.TimeLine.as_view(), name='timeline'),
    # path('', views.TimeLine.as_view(), name='post'),
    # path('write/', views.PostWrite.as_view(), name='post_write'),
    # path('update/<int:pk>/', views.PostUpdateView.as_view(), name='post_update/'),
    # path('post/<int:pk>/', views.detail ),
    # # path('<int:pk>/', views.detail ),
    # # path('delete/<int:id>/', views.delete ),
]

